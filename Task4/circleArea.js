const radius = 5;
const pi = Math.PI;
const area = pi * radius * radius;
const circumference = 2 * pi * radius;
const diameter = radius*2;

console.log(`Area: ${area}, Circumference: ${circumference}, Diameter: ${diameter}`);
